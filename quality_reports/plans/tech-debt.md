# ABOUTME: Tech debt discovered during planned work but not addressed, one line each
# ABOUTME: Each entry points back to the plan that surfaced it (rules/plan-first-workflow.md, Living Plans)

# Tech Debt

- 2026-07-27: the Review Artifacts gitignore guard is prose, not enforcement; a PreToolUse hook (or a test-e2e case) gating writes under `quality_reports/reviews/` on a target-repo ignore line would make it deterministic. From `plans/active/2026-07-27_swarm-forge-borrowings.md` (W2).
- 2026-07-27: the plan-template Depth column and COVERAGE footer are prompt-only devices; if they decay (falsification of the plan-depth contract: 2+ of the next 10 test-heavy plans ship without them), build a presence-check hook or test-e2e case. From `plans/active/2026-07-27_gstack-borrowings.md` (decision 7).
- 2026-07-27: gstack's scrape-to-skillify carries transferable patterns for a future skill-forge "codify" mode (stage/test/approve/atomic-rename lifecycle, bounded conversation walk-back, pure-parser/impure-driver split); deferred, coupled to gstack's $B browse daemon + bun. From `plans/active/2026-07-27_gstack-borrowings.md` (decision 13).
- 2026-07-27: forge-drift-check matches hook registration by filename substring only; settings.json matcher-format drift (right file registered under the wrong event or matcher) is a known gap. From `plans/active/2026-07-27_gstack-borrowings.md` (decision 10).
- 2026-07-28: scoped re-review (rounds 2+ reviewing only the fix diff instead of the whole change set) was designed and then DEFERRED: all three second-opinion reviewers independently ranked it the highest quality risk, since it is the only change that removes eyes from code and its failure mode (fix-induced regression in an untouched caller) is what inline review exists to catch. If revisited, the reviewers' guardrail is mandatory: if the fix diff touches any interface point (signature, export, schema, endpoint, shared fixture per the "shared integration surfaces" list), expand scope to depth-1 importers via the existing ast-grep pre-filter, and write the round's scope plus its non-reviewed remainder into the findings file's Verification gaps. BLAST-RADIUS is NOT sufficient cover: it is textual and conditional, so it misses same-signature behavioral regressions and skips exactly on the small diffs where scoped review is narrowest. From plans/active/2026-07-28_review-loop-cost.md (decision 5).
- 2026-07-29: `make check` does not shellcheck `hooks/*.sh`; `Makefile` `lint-shell` covers `install.sh get.sh scripts/pi-exec scripts/score-log.sh` only, so roughly 20 shell hooks are unlinted and a new one has to be checked by hand. Widening the glob was deliberately kept out of #116 (unrelated, unbounded diff). From `plans/active/2026-07-28_reviewer-isolation-guard.md` (decision 13).
- 2026-07-29: `reviewer-isolation-guard` enforces the `isolation` parameter but never checks whether the brief carries the `This launch carries isolation: "worktree"` assertion the reviewers actually gate their writes on, so a launch with the parameter and a brief without the sentence yields a silently degraded read-only review. A one-line stderr warn at the allow rung would make it observable; it is new hook behaviour and needs its own E2E row and contract. From `plans/active/2026-07-28_reviewer-isolation-guard.md` (decision 16, round-1 finding F8).
- 2026-07-28: reviewer-definition citation rules overlap (base-SHA anchor + quote-exact-code, decision 9) — dedup into one sentence when the block is next touched; uniformity hash guards meanwhile. From plans/completed/2026-07-29_reviewer-isolation.md.
