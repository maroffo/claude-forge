{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Project-scoped hooks for claude-forge sessions. checkpoint-reminder nudges the 5/10-session change-contract checkpoints; it is registered here (not in user settings) because traces and contracts live in this repo.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          { "type": "command", "command": "$CLAUDE_PROJECT_DIR/hooks/checkpoint-reminder.sh", "timeout": 10 }
        ]
      }
    ]
  }
}
